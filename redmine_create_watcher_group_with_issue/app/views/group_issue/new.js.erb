$("#dialog_add_groups").html("<%= escape_javascript(render :partial => 'group_issue/new', :locals => {:project => @project, :groups => @groups}) %>");

$("#dialog_add_groups").dialog({
   width: 400,
   resizable: true,
   modal: true
});